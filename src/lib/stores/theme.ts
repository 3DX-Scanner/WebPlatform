// Theme management is now handled by mode-watcher
// This file is kept for backward compatibility but the theme store is no longer used
// Components should import { theme } from 'mode-watcher' directly
// Re-export theme from mode-watcher for backward compatibility
export { theme } from 'mode-watcher';
